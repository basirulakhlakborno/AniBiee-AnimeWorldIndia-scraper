## 2026-02-25 - Static File Verification
**Learning:** Static HTML files in `public/` are not covered by the existing `lint` or `test` scripts and require custom verification methods (e.g., Playwright scripts).
**Action:** Create verification scripts for any changes in `public/` directory.
